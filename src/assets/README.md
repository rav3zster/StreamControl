# Source Assets (`src/assets`)

Place logos and images here that you want to import directly in your React components.

Example usage in React:
```tsx
import logo from '../assets/logo.png';

<img src={logo} alt="Logo" />
```
